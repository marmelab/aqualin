<html>
  <head>
    <meta charset="UTF-8" />
    <title>Aqualin</title>
    <link rel="stylesheet" type="text/css" href="/css/index.css" />
  </head>

  <body>
    <div id="page">
      <h1>Aqualin</h1>
      <div id="game">
        <div id="playerSide">
          Turn : {{ gameState.playerTurn }}
        </div>
        <div id="boardSide">
          <h2>Board</h2>
          <div>{{message}}</div>
          <div class="board">
            {{#each gameState.board}}
              {{#each this}}
                {{! call renderCell with the game State, the row index and column index
                 we need to go up twice to recover the gameState, "this" is the cell}} 
                {{#renderCell this ../../gameState @../index @index}}{{/renderCell}}
              {{/each}}
            {{/each}}
          </div>
        </div>
        <div id="riverSide">
          <h2>River</h2>
          <div id="river">
            {{#each gameState.river}}
              {{#renderRiver this ../gameState @index}}{{/renderRiver}}
            {{/each}}
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
